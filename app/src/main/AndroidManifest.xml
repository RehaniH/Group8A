<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.group8a">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShoppingActivity"></activity>
        <activity android:name=".AdminActivity" />
        <activity android:name=".DisplayProductActivity" />
        <activity android:name=".ProductActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".ResetPasswordActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".AdminProductActivity" />
        <activity android:name=".deliveryoptions"/>
        <activity android:name=".finalview"/>
        <activity android:name=".giftting_options"/>
        <activity android:name="HeadlineFragment"/>
        <activity android:name=".newaddress"/>
        <activity android:name=".Payments"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>